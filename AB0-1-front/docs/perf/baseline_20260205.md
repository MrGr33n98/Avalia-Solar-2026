# Baseline Performance Snapshot — AB0-1 Front

**Date:** 2026-02-05
**Scope:** T00_BASELINE_FREEZE
**Environment:** Local Windows runner

## Build/Analyze Status
- `npm run build`: **Timed out** after 120s (no output captured).
- `npm run analyze`: **Failed** with OOM, but **bundle analyzer reports were generated**:
  - `.next/analyze/client.html`
  - `.next/analyze/nodejs.html`
  - `.next/analyze/edge.html`

## Bundle Reports
- Reports exist on disk. Parsing the HTML for top offenders **timed out** during extraction.
- Next step: parse reports using a larger Node heap or an external tool.

## Lighthouse
- Not run (requires local server + repeated runs). Pending.

## Known Warnings from Analyze Build
- OpenTelemetry / `require-in-the-middle` critical dependency warnings via Sentry instrumentation.

## Missing Data (blocked)
- Top gzip chunks from `client.html`
- Route-level First Load JS sizes
- Lighthouse metrics (LCP/TBT/CLS)

## TODO to complete baseline
1. Re-run `npm run analyze` with larger heap: `NODE_OPTIONS=--max-old-space-size=8192`.
2. Parse `client.html` via bundle analyzer UI or a script on a machine with more RAM.
3. Start app and run Lighthouse 3x for `/`.
