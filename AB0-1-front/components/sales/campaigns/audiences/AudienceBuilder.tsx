import { Input } from '@/components/ui/input';
import { Button } from '@/components/ui/button';
import { AudienceSegmentsOptions } from '@/lib/api-campaigns';
type Filter = { state: string; city: string; segment: string; search: string; tag_ids: number[] };
type Props = { filter: Filter; segments: AudienceSegmentsOptions | null; name: string; onNameChange: (value: string) => void; onFilterChange: (key: keyof Filter, value: string) => void; onSave: () => void; saving: boolean };
export function AudienceBuilder({ filter, segments, name, onNameChange, onFilterChange, onSave, saving }: Props) { const cityOptions = filter.state ? (segments?.cities_by_state?.[filter.state] || []) : (segments?.cities || []); const selectedTags = filter.tag_ids || []; return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-6"><div className="mb-5 flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-blue-600">Audience builder</p><h2 className="mt-1 text-lg font-semibold text-slate-950">Defina quem deve receber</h2><p className="mt-1 text-sm text-slate-500">Filtros dinâmicos respeitando contatos elegíveis e suppressions.</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs text-slate-600">{Object.values(filter).filter(Boolean).length} ativos</span></div><div className="grid gap-4 sm:grid-cols-2">{([['state','Estado','states'],['city','Cidade','cities'],['segment','Tipo de empresa','company_types']] as const).map(([key,label,optionKey]) => <label key={key} className="text-sm font-medium text-slate-700">{label}<select aria-label={label} className="mt-1 block h-10 w-full rounded-md border border-slate-200 bg-white px-3 text-sm focus:outline-none focus:ring-2 focus:ring-blue-600" value={filter[key]} onChange={(event) => onFilterChange(key,event.target.value)}><option value="">Todos</option>{(key === 'city' ? cityOptions : (segments?.[optionKey] || [])).map((value) => <option key={value}>{value}</option>)}</select></label>)}<label className="text-sm font-medium text-slate-700">Nome ou e-mail<Input className="mt-1" value={filter.search} onChange={(event) => onFilterChange('search', event.target.value)} placeholder="Buscar contato..." /></label></div><div className="mt-4"><span className="text-sm font-medium text-slate-700">Tags</span><div className="mt-2 flex flex-wrap gap-2">{(segments?.tags || []).map((tag) => <button key={tag.id} type="button" aria-pressed={selectedTags.includes(tag.id)} className={`rounded-full border px-3 py-1 text-xs transition-colors ${selectedTags.includes(tag.id) ? 'border-blue-600 bg-blue-50 text-blue-700' : 'border-slate-200 bg-white text-slate-600 hover:border-slate-400'}`} onClick={() => onFilterChange('tag_ids', String(tag.id))}>{tag.name}</button>)}{!segments?.tags?.length && <span className="text-xs text-slate-400">Nenhuma tag disponível</span>}</div></div><div className="mt-6 flex flex-col gap-3 border-t border-slate-100 pt-5 sm:flex-row sm:items-end"><label className="flex-1 text-sm font-medium text-slate-700">Nome da audiência<Input className="mt-1" value={name} onChange={(event) => onNameChange(event.target.value)} placeholder="Ex.: Integradores RS" /></label><Button className="bg-slate-950 hover:bg-slate-800" disabled={!name.trim() || saving} onClick={onSave}>{saving ? 'Salvando...' : 'Salvar audiência'}</Button></div></section>; }
