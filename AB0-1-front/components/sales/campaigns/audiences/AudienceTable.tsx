import { AudiencePreviewResult } from '@/lib/api-campaigns';
export function AudienceTable({ data }: { data: AudiencePreviewResult }) { if (!data.sample_contacts.length) return <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-10 text-center"><p className="font-semibold text-slate-900">Nenhum contato encontrado</p><p className="mt-1 text-sm text-slate-500">Remova um filtro ou amplie sua busca.</p></div>; return <div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white shadow-sm"><table className="w-full text-left text-sm"><caption className="sr-only">Prévia de contatos elegíveis</caption><thead className="border-b bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr><th scope="col" className="px-4 py-3">Contato</th><th scope="col" className="px-4 py-3">E-mail</th><th scope="col" className="px-4 py-3">Empresa</th><th scope="col" className="px-4 py-3">Localização</th></tr></thead><tbody className="divide-y divide-slate-100">{data.sample_contacts.map((contact) => <tr key={contact.id} className="hover:bg-slate-50"><td className="px-4 py-3 font-medium text-slate-900">{contact.first_name} {contact.last_name}</td><td className="px-4 py-3 text-slate-600">{contact.email}</td><td className="px-4 py-3 text-slate-600">{contact.account_name || '—'}</td><td className="px-4 py-3 text-slate-600">{[contact.city, contact.state].filter(Boolean).join(', ') || '—'}</td></tr>)}</tbody></table></div>; }
