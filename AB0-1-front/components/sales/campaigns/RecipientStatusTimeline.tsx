import { CampaignRecipientLog } from '@/lib/api-campaigns';
const steps: Array<[keyof CampaignRecipientLog, string]> = [['sent_at', 'Enviado'], ['delivered_at', 'Entregue'], ['opened_at', 'Aberto'], ['clicked_at', 'Clicado']];
export function RecipientStatusTimeline({ recipient }: { recipient: CampaignRecipientLog }) { return <ol aria-label={`Linha do tempo de ${recipient.email}`} className="flex min-w-[360px] items-center gap-1 text-[11px]">{steps.map(([field, label], index) => <li key={field} className="flex items-center gap-1"><span className={`grid h-5 w-5 place-items-center rounded-full ${recipient[field] ? 'bg-emerald-100 text-emerald-700' : 'bg-slate-100 text-slate-400'}`}>{recipient[field] ? '✓' : '·'}</span><span className={recipient[field] ? 'text-slate-700' : 'text-slate-400'}>{label}</span>{index < steps.length - 1 && <span className="mx-1 text-slate-300">/</span>}</li>)}</ol>; }
