# Story 5.0: Analytics Reliability Roadmap (P0/P1/P2)

## Status
- **Status**: Planned
- **Executor**: @data-engineer + @dev + @devops + @qa
- **Quality Gate**: @architect

---

## Objetivo
Garantir tracking, attribution, telemetria e métricas de dashboard com confiabilidade operacional para decisão de negócio, com fonte canônica por endpoint, reconciliação automática e observabilidade contínua.

---

## Escopo
- Ingestão de eventos (`/api/v1/analytics/track`, `/api/v1/events/track`)
- Attribution (UTM, session, first/last touch)
- Persistência (`platform_events`, `analytics_events`, `leads`)
- Agregação (`company_daily_stats`)
- Dashboards (`company_dashboard`, `analytics/conversions`)

---

## Fonte Canônica por Endpoint
| Endpoint | Fonte canônica | Fallback permitido |
|----------|----------------|-------------------|
| `GET /api/v1/company_dashboard/analytics/overview` | `company_daily_stats` | payload zerado com `data_source=company_daily_stats_unavailable` |
| `GET /api/v1/company_dashboard/analytics/timeseries` | `company_daily_stats` | lista vazia |
| `GET /api/v1/company_dashboard/stats` | `company_daily_stats` (engajamento) + `leads` (leads_received) | contadores denormalizados |
| `GET /api/v1/analytics/conversions?company_id=` | core via `company_daily_stats` + não-core via `analytics_events` | `analytics_events` puro |
| `GET /api/v1/analytics/conversions` (sem `company_id`) | `analytics_events` | sem fallback |

---

## SLOs / SLIs
1. **Ingestão de evento**
- SLI: `% eventos aceitos`
- SLO: `>= 99.5%` em 24h

2. **Atraso de agregação**
- SLI: `now - last_processed_at (analytics_processing_state)`
- SLO: `<= 15 min` (P95)

3. **Reconciliação de métricas core**
- SLI: `delta(company_daily_stats vs analytics_events/leads)`
- SLO: `<= 2%` por empresa/dia

4. **Cobertura de attribution em leads**
- SLI: `% leads com utm_source OU attribution_json.first_touch`
- SLO: `>= 95%` (para tráfego de campanhas)

---

## Execução por Fase

### P0 (7 dias) - Confiabilidade mínima de produção
**Owner principal:** Data Engineer  
**Copilotos:** Backend, QA

1. Ativar monitor de divergência diário (job) entre:
- `company_daily_stats.profile_views` vs `analytics_events(profile_view)`
- `company_daily_stats.leads` vs `leads(created_at/day)`

**Aceite**
- job executa diariamente
- grava resultado em tabela/log estruturado
- alerta quando delta > 2%

2. Publicar metadados de frescor nos endpoints de dashboard:
- `data_source`
- `data_freshness_seconds` (ou `last_aggregated_at`)

**Aceite**
- todos endpoints de dashboard retornam campo de frescor
- frontend exibe estado “dados desatualizados” quando acima do limite

3. Validar contrato mínimo de evento no backend (sem breaking change):
- `event_type`
- `event_id`
- `tracked_at`
- `session_id` (quando disponível)

**Aceite**
- eventos inválidos são rejeitados ou marcados com erro explícito
- logs com motivo de rejeição

4. Rodar backfill controlado dos últimos 30 dias em `company_daily_stats`

**Aceite**
- execução idempotente
- sem duplicidade de contagem
- relatório de antes/depois da divergência

---

### P1 (15 dias) - Qualidade de tracking e governança de métrica
**Owner principal:** Backend  
**Copilotos:** Frontend, Data Engineer, QA

1. Dicionário canônico de eventos e métricas (versionado)
- evento
- definição
- origem
- endpoint consumidor
- regra de agregação

**Aceite**
- documento versionado
- nenhum endpoint crítico sem mapeamento

2. Suite de testes E2E de tracking (jornadas críticas):
- page_view
- cta_click
- whatsapp_click
- wizard submit
- otp verify
- success

**Aceite**
- casos automatizados para jornada de empresa e categoria
- validação de payload (`session_id`, attribution, company_id/category_id)

3. Guardrails de CI para telemetria:
- falhar CI se evento core perder campos obrigatórios
- smoke de endpoints de analytics/dashboards

**Aceite**
- pipeline bloqueia regressão de tracking core

4. Padronização de nomenclatura de eventos (deprecar aliases legados)

**Aceite**
- mapa de aliases -> canonical
- monitor de tráfego para aliases em depreciação

---

### P2 (30-45 dias) - Escala e resiliência estrutural
**Owner principal:** DevOps + Data Engineer  
**Copilotos:** Backend, Architect

1. Pipeline de serving analytics formal:
- ingestão
- agregação
- serving table/materialização
- observabilidade

**Aceite**
- runbook publicado
- métricas do pipeline em dashboard técnico

2. Política de retenção e particionamento para tabelas de evento

**Aceite**
- estratégia definida por tabela
- custo e performance medidos

3. Plano de evolução para camada analítica dedicada (quando volume justificar)

**Aceite**
- gatilhos objetivos de migração (volume/latência/custo)
- plano de cutover sem downtime analítico

---

## Backlog Técnico Priorizado
### P0
- [x] Job `analytics_reconciliation_daily`
- [x] Campo de frescor em endpoints do dashboard
- [ ] Backfill 30d com relatório de delta
- [x] Alerta operacional de divergência

### P1
- [ ] Documento de dicionário de eventos e métricas
- [ ] Testes E2E de tracking core
- [ ] Guardrails de CI para payload de analytics
- [ ] Depreciação de aliases de evento

### P2
- [ ] Blueprint de pipeline analítico de serving
- [ ] Política de retenção/particionamento
- [ ] Plano de evolução para camada analítica dedicada

---

## File List
- [x] `AB0-1-back/db/migrate/20260305100000_create_analytics_reconciliations.rb`
- [x] `AB0-1-back/app/models/analytics_reconciliation.rb`
- [x] `AB0-1-back/app/jobs/analytics/daily_reconciliation_job.rb`
- [x] `AB0-1-back/app/services/company_dashboard/freshness_provider.rb`
- [x] `AB0-1-back/app/controllers/api/v1/company_dashboard_controller.rb`
- [x] `AB0-1-back/app/services/company_dashboard/stats_service.rb`
- [x] `AB0-1-back/spec/jobs/analytics/daily_reconciliation_job_spec.rb`
- [x] `AB0-1-back/spec/services/company_dashboard/freshness_provider_spec.rb`
- [x] `AB0-1-back/spec/requests/api/v1/company_dashboard_freshness_spec.rb`

---

## Critérios de Go/No-Go
**Go para comercial escala**
- SLO ingestão e reconciliação cumpridos por 14 dias
- divergência core < 2% em P95
- sem regressão de tracking core em 2 ciclos de release

**No-Go**
- divergência recorrente > 5%
- backlog de agregação acima do SLO
- falta de cobertura de eventos críticos no funil

---

## Validation
- [x] `ruby -c` check em todos os novos arquivos
- [x] RSpec unitários e request criados para cobertura P0
- [ ] `bundle exec rspec` (bloqueado no ambiente atual)
- [ ] checks SQL de reconciliação em staging
- [ ] validação manual dos dashboards com `data_source` e frescor

---

## Change Log
| Data       | Versão | Descrição | Autor |
|------------|--------|-----------|-------|
| 2026-03-05 | 1.0    | Roadmap executável P0/P1/P2 para tracking, analytics e telemetria | Codex |
