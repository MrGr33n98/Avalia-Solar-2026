# Chat Alerts

`Chat::AlertingService` cobre LLM errors, SSE failures, TTFT degradation, lead capture, webhook enqueue, ActionCable reconnect, token verification e rate limiting. Eventos não interrompem fluxo principal.
