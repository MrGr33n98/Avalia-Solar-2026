# Inventário UI

- Existentes: `Person360FullView`, `UnifiedTimeline`, `EmailCenterPage`, dois `EmailComposerModal`, `SalesLayoutWrapper`, `CRMSidebar`, modais de atividade/tarefa/contato.
- Reutilizáveis: `Button`, `Badge`, `Input`, `Textarea`, `CRMModal`, `CRMForm`.
- Faltantes para alvo: `CRMSectionShell`, `CRMAccordionCard`, density tokens, `PersonHeader`, `QuickActionBar`, `LocalSectionNav`, context rail, engagement card e template picker/editor.
- Risco: há componentes duplicados em `components/sales` e `components/sales/email`; consolidar somente com imports verificados.
