# Plano de testes — e-mail

RSpec: SES normalization, idempotência, provider failure, tenant isolation, templates e suppression. Jest/RTL: composer, picker, editor, states e engagement. E2E: contato, envio, `provider_message_id`, delivered, open/click e timeline. Executar typecheck, lint, Jest e RSpec quando toolchains estiverem disponíveis.
