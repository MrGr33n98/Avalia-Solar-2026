# Procfile for development - TASK-017
# Use with: foreman start -f Procfile.dev
# Or: overmind start (recommended)

web: bundle exec rails server -p 3001
worker: bundle exec sidekiq -C config/sidekiq.yml
redis: redis-server
