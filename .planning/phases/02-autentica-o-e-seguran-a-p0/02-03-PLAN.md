---
phase: 02-autentica-o-e-seguran-a-p0
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/components/LoginGate.tsx
  - src/hooks/useProtectedAction.ts
  - src/components/__tests__/LoginGate.test.ts
  - src/app/profile.tsx
  - src/app/__tests__/profile.test.tsx
autonomous: false
requirements:
  - AUTH-01
must_haves:
  truths:
    - "Ações e componentes protegidos requerem usuário logado"
    - "O usuário recebe tela de login/redirect ao tentar burlar o acesso"
    - "O formulário de login real está conectado e processa a autenticação com sucesso ou erro"
  artifacts:
    - path: "src/hooks/useProtectedAction.ts"
      provides: "Hook utilitário para checar login programaticamente"
    - path: "src/components/LoginGate.tsx"
      provides: "HOC/Wrapper JSX de proteção"
    - path: "src/app/profile.tsx"
      provides: "Integração UI para o formulário de login real"
  key_links:
    - from: "src/components/LoginGate.tsx"
      to: "src/store/auth.ts"
      via: "Leitura de estado reativo"
      pattern: "useAuthStore"
    - from: "src/app/profile.tsx"
      to: "src/store/auth.ts"
      via: "Ação de login e leitura de erros"
      pattern: "login"
---

<objective>
Proteger rotas e ações da UI, integrando o novo gerenciamento de sessão real na interface, incluindo o formulário de login.

Purpose: Amarrar o sistema robusto construído nos planos anteriores com os fluxos do usuário e conectar o formulário real.
Output: Utilitários de restrição (`LoginGate` e `useProtectedAction`), formulário conectado e checkpoint manual finalizado.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-autentica-o-e-seguran-a-p0/02-CONTEXT.md
@.planning/phases/02-autentica-o-e-seguran-a-p0/02-VALIDATION.md

# Referência do plano anterior
@.planning/phases/02-autentica-o-e-seguran-a-p0/02-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `src/components/LoginGate.tsx`
- `src/hooks/useProtectedAction.ts`
- `src/components/__tests__/LoginGate.test.ts`
- `src/app/__tests__/profile.test.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Protected Gate Implementation</name>
  <files>src/components/LoginGate.tsx, src/hooks/useProtectedAction.ts, src/components/__tests__/LoginGate.test.ts</files>
  <read_first>AB0-1-mobile/src/store/auth.ts</read_first>
  <acceptance_criteria>LoginGate renders content conditionally based on auth store. tests pass.</acceptance_criteria>
  <action>
    - Crie o `src/hooks/useProtectedAction.ts` que retorna uma função acionadora. Se o usuário estiver logado, a função embutida executa. Se não, invoca redirecionamento.
    - Crie o `src/components/LoginGate.tsx` que monitora `useAuthStore`. Se o auth for falso, exibe componente custom (modal) ou redireciona e exibe loading.
    - Escreva os testes para o `LoginGate` conforme definido na estratégia Wave 0 de testes de componente.
  </action>
  <verify>
    <automated>npm test -- LoginGate</automated>
  </verify>
  <done>Hook and Component created successfully with tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Profile Login UI Integration</name>
  <files>src/app/profile.tsx, src/app/__tests__/profile.test.tsx</files>
  <read_first>AB0-1-mobile/src/app/profile.tsx</read_first>
  <acceptance_criteria>Profile page successfully handles real login using the updated authApi store method, showing validation errors correctly.</acceptance_criteria>
  <action>
    - Refatore `src/app/profile.tsx` (formulário de Login real) para assegurar a integração com a store de autenticação e a nova infraestrutura de rede (API real).
    - Garanta o correto tratamento de erros da API (ex: senhas incorretas, conta inativa) para exibição amigável ao usuário.
    - Crie/atualize testes de componente (`src/app/__tests__/profile.test.tsx`) cobrindo o comportamento de submissão do formulário.
  </action>
  <verify>
    <automated>npm test -- profile</automated>
  </verify>
  <done>O formulário de login real está conectado à API e tratando erros corretamente.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: End-to-End Auth Validation</name>
  <files>None</files>
  <read_first>None</read_first>
  <acceptance_criteria>Human verifies that the entire auth flow (login, token persist, 401 logout, protected action block) functions correctly on device/simulator.</acceptance_criteria>
  <action>
    Aguardar a verificação humana da interface conectada à API usando credenciais reais.
  </action>
  <verify>
    <automated>MISSING — Wave 0 must create src/app/__tests__/e2e.test.ts first</automated>
  </verify>
  <done>Integração UI validada.</done>
  <how-to-verify>
    1. Deslogue caso esteja logado.
    2. Clique em Favoritar uma Empresa (ou navegue em rota protegida); observe o gate de acesso (Modal ou Tela de Login).
    3. Efetue login válido através do formulário real em Perfil; verifique armazenamento da sessão local.
    4. Feche e abra o App; garanta que continua logado.
  </how-to-verify>
  <resume-signal>Aprove respondendo "approved" ou aponte os erros de navegação.</resume-signal>
</task>

</tasks>

<verification>
Toda navegação autenticada requer login genuíno real; gate redireciona apropriadamente e formulário responde à API real.
</verification>

<success_criteria>
- `npm test -- LoginGate` roda e passa sem erros.
- `npm test -- profile` roda e passa sem erros.
- Verificação visual/interação aceita pelo usuário.
</success_criteria>

<output>
After completion, create `.planning/phases/02-autentica-o-e-seguran-a-p0/02-03-SUMMARY.md`
</output>