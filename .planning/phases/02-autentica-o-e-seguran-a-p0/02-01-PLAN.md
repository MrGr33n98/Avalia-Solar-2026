---
phase: 02-autentica-o-e-seguran-a-p0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/api.ts
  - src/store/auth.ts
  - src/lib/__tests__/authStorage.test.ts
  - src/store/__tests__/authStore.test.ts
  - src/app/__tests__/e2e.test.ts
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "Sessão carrega corretamente e persiste o acesso do usuário"
  artifacts:
    - path: "src/store/auth.ts"
      provides: "Gerenciamento de estado de Auth isolado e testável"
    - path: "src/lib/__tests__/authStorage.test.ts"
      provides: "Validação da persistência de tokens (Wave 0)"
  key_links:
    - from: "src/store/auth.ts"
      to: "src/lib/api.ts"
      via: "Chamadas isoladas para não gerar loop cíclico"
      pattern: "authApi"
---

<objective>
Refatorar a base da store de autenticação para eliminar mocks e resolver o risco de dependências circulares com a camada de API, além de cobrir o storage com testes unitários.

Purpose: Criar uma fundação de autenticação estável e testável para que os interceptadores de rede possam acionar o estado de login/logout com segurança.
Output: Dependências estruturadas e testes fundamentais.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-autentica-o-e-seguran-a-p0/02-CONTEXT.md
@.planning/phases/02-autentica-o-e-seguran-a-p0/02-VALIDATION.md

<interfaces>
From src/store/auth.ts:
```typescript
interface AuthState {
  user: User | null;
  token: string | null;
  isLoading: boolean;
  logout: () => Promise<void>;
}
```
</interfaces>
</context>

<artifacts_this_phase_produces>
- `src/lib/__tests__/authStorage.test.ts`
- `src/store/__tests__/authStore.test.ts`
- `src/app/__tests__/e2e.test.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create E2E Test Stub (Wave 0)</name>
  <files>src/app/__tests__/e2e.test.ts</files>
  <read_first></read_first>
  <acceptance_criteria>The e2e test stub file exists for subsequent manual verification steps.</acceptance_criteria>
  <action>
    - Crie o arquivo `src/app/__tests__/e2e.test.ts` e exporte um teste vazio ou apenas a estrutura base (describe block) para que os checkpoints das tarefas seguintes tenham para onde referenciar a automação no futuro.
  </action>
  <verify>
    <automated>test -f src/app/__tests__/e2e.test.ts || echo "Stub missing"</automated>
  </verify>
  <done>E2E stub created</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Secure Storage Verification (Wave 0)</name>
  <files>src/lib/__tests__/authStorage.test.ts</files>
  <read_first>AB0-1-mobile/src/lib/authStorage.ts</read_first>
  <acceptance_criteria>Tests correctly assert that the SecureStore methods are being wrapped effectively for token persistence.</acceptance_criteria>
  <action>
    - Crie os testes unitários (`src/lib/__tests__/authStorage.test.ts`) cobrindo as funções atuais (`getStoredToken`, `setStoredToken`, `removeStoredToken`) para validar o isolamento e persistência segura, utilizando mocks do Jest para o `expo-secure-store`.
  </action>
  <verify>
    <automated>npm test -- authStorage</automated>
  </verify>
  <done>authStorage test suite passes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Store Stability and Circular Dependency Removal</name>
  <files>src/lib/api.ts, src/store/auth.ts, src/store/__tests__/authStore.test.ts</files>
  <read_first>AB0-1-mobile/src/lib/api.ts, AB0-1-mobile/src/store/auth.ts</read_first>
  <acceptance_criteria>auth.ts no longer inherently conflicts with api.ts at module load time, enabling lazy evaluation of store state in interceptors. Store logic is verified through passing unit tests.</acceptance_criteria>
  <action>
    - Refatore `src/lib/api.ts` e/ou `src/store/auth.ts` garantindo que não há problemas de inicialização cruzada (pode-se usar lazy init do Zustand com `useAuthStore.getState()` nos interceptadores posteriores).
    - Assegure-se que não existe "mock" do lado da inicialização (verifique o inicializador atual da store).
    - Implemente a bateria de testes requerida em `src/store/__tests__/authStore.test.ts` (conforme Wave 0 stubs).
  </action>
  <verify>
    <automated>npm test -- authStore</automated>
  </verify>
  <done>authStore dependencies resolved and tests pass.</done>
</task>

</tasks>

<verification>
A arquitetura base (Storage e Store) está limpa, livre de dependências circulares e conta com a cobertura mínima (Wave 0). O stub E2E foi gerado com sucesso para suportar o plano final.
</verification>

<success_criteria>
- Arquivo `src/app/__tests__/e2e.test.ts` existe.
- `npm test -- authStorage` passa com sucesso.
- `npm test -- authStore` passa com sucesso.
</success_criteria>

<output>
After completion, create `.planning/phases/02-autentica-o-e-seguran-a-p0/02-01-SUMMARY.md`
</output>