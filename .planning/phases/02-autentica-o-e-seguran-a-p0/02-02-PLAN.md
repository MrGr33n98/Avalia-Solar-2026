---
phase: 02-autentica-o-e-seguran-a-p0
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/lib/api.ts
  - src/lib/apolloClient.ts
  - src/lib/__tests__/apolloClientAuth.test.ts
autonomous: true
requirements:
  - AUTH-01
must_haves:
  truths:
    - "Usuário é deslogado automaticamente quando a API retorna 401"
  artifacts:
    - path: "src/lib/api.ts"
      provides: "REST Interceptor de 401 (fetchApi)"
    - path: "src/lib/apolloClient.ts"
      provides: "GraphQL Interceptor de 401 via onError"
  key_links:
    - from: "src/lib/apolloClient.ts"
      to: "src/store/auth.ts"
      via: "onError link acionando logout global"
      pattern: "logout"
---

<objective>
Implementar a interceptação global na camada de rede (REST e GraphQL) para forçar o logout sempre que o servidor acusar token inválido/expirado (401 Unauthorized).

Purpose: Manter a coerência de acesso da sessão, revogando privilégios e limpando estado imediatamente em falhas de autorização.
Output: Network interceptors funcionando para ambos clientes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-autentica-o-e-seguran-a-p0/02-CONTEXT.md
@.planning/phases/02-autentica-o-e-seguran-a-p0/02-VALIDATION.md

# Referência do plano anterior
@.planning/phases/02-autentica-o-e-seguran-a-p0/02-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `src/lib/__tests__/apolloClientAuth.test.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GraphQL & REST 401 Interceptors</name>
  <files>src/lib/api.ts, src/lib/apolloClient.ts, src/lib/__tests__/apolloClientAuth.test.ts</files>
  <read_first>AB0-1-mobile/src/lib/api.ts, AB0-1-mobile/src/lib/apolloClient.ts</read_first>
  <acceptance_criteria>401 status in fetchApi triggers authStore logout. GraphQL UNAUTHENTICATED or 401 networkError triggers authStore logout.</acceptance_criteria>
  <action>
    - Em `fetchApi` (`src/lib/api.ts`), capture o `response.status === 401` e chame `useAuthStore.getState().logout()` (usando a injeção apropriada feita no Plan 01 para não gerar loops de import).
    - Em `apolloClient.ts`, adicione o middleware do `@apollo/client/link/error` e repita o fluxo para erros GraphQLErrors `UNAUTHENTICATED` e NetworkError (status 401).
    - Crie o arquivo de testes `apolloClientAuth.test.ts` de integração verificando esse comportamento de logout em cenários de 401.
  </action>
  <verify>
    <automated>npm test -- apolloClient</automated>
  </verify>
  <done>Os clientes interceptam 401 e forçam logout adequadamente, testes passando.</done>
</task>

</tasks>

<verification>
Qualquer chamada com credenciais expiradas deve esvaziar a store e storage local simultaneamente.
</verification>

<success_criteria>
- `npm test -- apolloClient` roda e passa sem erros.
</success_criteria>

<output>
After completion, create `.planning/phases/02-autentica-o-e-seguran-a-p0/02-02-SUMMARY.md`
</output>