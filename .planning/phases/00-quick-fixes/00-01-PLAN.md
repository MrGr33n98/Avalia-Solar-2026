---
phase: 00-quick-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "AB0-1-front/components/chat/ChatWidget.tsx",
  "AB0-1-front/lib/analytics/cookies.ts",
  "AB0-1-front/lib/analytics/gtag.ts",
  "AB0-1-front/lib/posthog.ts"
]
autonomous: true
requirements: [FIX-01, FIX-02]
must_haves:
  truths:
    - "ChatWidget.tsx imports and uses useCallback without ReferenceError"
    - "Cookie utility supports domain option"
    - "Analytics cookies are configured with explicit domain to avoid rejection"
  artifacts:
    - path: "AB0-1-front/components/chat/ChatWidget.tsx"
      provides: "Working chat widget"
    - path: "AB0-1-front/lib/analytics/cookies.ts"
      provides: "Enhanced cookie utility"
---

<objective>
Fix the ReferenceError in ChatWidget and address cookie domain rejection issues by ensuring consistent domain configuration across analytics tools.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
- User reported `ReferenceError: useCallback is not defined` in `ChatWidget.tsx`.
- User reported cookie rejection for `www.avaliasolar.com.br` for GA and PostHog (`dmn_chk`).
- `AB0-1-front/lib/analytics/cookies.ts` currently lacks a `domain` option.
</context>

<tasks>

<task type="auto">
  <name>Fix useCallback import in ChatWidget.tsx</name>
  <files>AB0-1-front/components/chat/ChatWidget.tsx</files>
  <action>
    Add `useCallback` to the named imports from 'react' at the top of the file.
    Change: `import { useState, useEffect, useRef } from 'react';`
    To: `import { useState, useEffect, useRef, useCallback } from 'react';`
  </action>
  <verify>
    <automated>grep "useCallback" AB0-1-front/components/chat/ChatWidget.tsx</automated>
  </verify>
  <done>ChatWidget.tsx should no longer throw ReferenceError for useCallback.</done>
</task>

<task type="auto">
  <name>Enhance cookie utility and fix domain rejection</name>
  <files>AB0-1-front/lib/analytics/cookies.ts, AB0-1-front/lib/analytics/gtag.ts, AB0-1-front/lib/posthog.ts</files>
  <action>
    1. In `AB0-1-front/lib/analytics/cookies.ts`:
       - Update `CookieOptions` type to include `domain?: string`.
       - Update `setCookie` to include `domain` in the cookie string if provided.
       - Update `deleteCookie` to also accept options or at least handle domain if necessary (standard practice is to delete with same domain/path used to set).
    2. In `AB0-1-front/lib/analytics/gtag.ts`:
       - Ensure `cookie_domain: 'auto'` is consistent, or if issues persist, allow it to be overridden. For now, keep as is or set to '.avaliasolar.com.br' if specified by user preference for permissive subdomains.
    3. In `AB0-1-front/lib/posthog.ts`:
       - Change `cross_subdomain_cookie` to `true` to allow PostHog to set cookies on the root domain, which often resolves `dmn_chk` rejection on subdomains.
  </action>
  <verify>
    <automated>npm run build --prefix AB0-1-front</automated>
  </verify>
  <done>Cookie setting logic is more robust and analytics tools are configured to handle subdomains correctly.</done>
</task>

</tasks>

<verification>
Verify that the frontend builds without errors and that the chat widget is functional.
</verification>

<success_criteria>
- No ReferenceError on page load.
- No "invalid domain" warnings in console for cookies on www.avaliasolar.com.br.
</success_criteria>

<output>
After completion, create `.planning/phases/00-quick-fixes/00-01-SUMMARY.md`
</output>
