---
phase: 01-funda-o-e-integra-o-da-home-p0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AB0-1-mobile/src/lib/queries/home.ts
  - AB0-1-mobile/src/components/skeleton/HomeSkeleton.tsx
  - AB0-1-mobile/src/app/index.tsx
autonomous: true
requirements: [HOME-01, MOCK-01]
must_haves:
  truths:
    - "A infraestrutura de dados para a Home está definida via GraphQL Fragments"
    - "Placeholders visuais (Skeletons) existem para todos os blocos dinâmicos"
  artifacts:
    - path: "AB0-1-mobile/src/lib/queries/home.ts"
      provides: "Queries e Fragments GQL para Home"
    - path: "AB0-1-mobile/src/components/skeleton/HomeSkeleton.tsx"
      provides: "Layout de carregamento da Home"
  key_links:
    - from: "AB0-1-mobile/src/app/index.tsx"
      to: "AB0-1-mobile/src/lib/queries/home.ts"
      via: "import { GET_HOME_DATA }"
---

<objective>
Estabelecer os contratos de dados (queries/fragments) e os placeholders de UI (skeletons) necessários para suportar o carregamento dinâmico da Home sem depender de mocks estáticos.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<interfaces>
Do backend (AB0-1-back/app/graphql/types/query_type.rb):
- field :banners, [Types::BannerType]
- field :categories, [Types::CategoryType]
- field :companies, Types::CompaniesConnectionType
- field :articles, Types::ArticlesConnectionType
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Definir contratos GQL e Stubs de Teste</name>
  <read_first>AB0-1-back/app/graphql/types/query_type.rb</read_first>
  <files>AB0-1-mobile/src/lib/queries/home.ts, AB0-1-mobile/src/features/home/__tests__/HomeQueries.test.ts</files>
  <action>
    Criar o arquivo de queries centralizadas para a Home.
    - Definir fragmentos para `Company`, `Category`, `Banner` e `Article` para garantir consistência.
    - Implementar a query `GET_HOME_DATA` que agrupa banners, categorias, empresas em destaque e artigos recentes em uma única chamada.
    - Criar stub de teste para validar a estrutura da query (Nyquist Wave 0).
  </action>
  <verify>
    <automated>npm test AB0-1-mobile/src/features/home/__tests__/HomeQueries.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `home.ts` exporta `GET_HOME_DATA`.
    - Query inclui campos: id, name, logoUrl (para empresas), imageUrl (para banners), slug (para categorias).
    - Teste de estrutura passa com sucesso.
  </acceptance_criteria>
  <done>Contratos de dados definidos e validados.</done>
</task>

<task type="auto">
  <name>Task 1: Implementar Skeletons com Efeito Shimmer</name>
  <read_first>AB0-1-mobile/src/constants/theme.ts, AB0-1-mobile/src/app/index.tsx</read_first>
  <files>AB0-1-mobile/src/components/skeleton/HomeSkeleton.tsx, AB0-1-mobile/src/components/skeleton/CompanyCardSkeleton.tsx</files>
  <action>
    Desenvolver os componentes de Skeleton para a Home seguindo o UI-SPEC.
    - Usar `react-native-reanimated` ou animação de opacidade simples para o efeito shimmer.
    - `CompanyCardSkeleton`: Imagem quadrada + 2 linhas de texto.
    - `HomeSkeleton`: Estrutura vertical simulando Header, Banners, Categorias e Grid de Empresas.
    - Respeitar os tokens de design (BorderRadius: 12, Spacing: 16).
  </action>
  <verify>
    <automated>ls AB0-1-mobile/src/components/skeleton/HomeSkeleton.tsx AB0-1-mobile/src/components/skeleton/CompanyCardSkeleton.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Componentes exportados e funcionais.
    - Layout visual corresponde à estrutura de seções do `index.tsx`.
    - Shimmer effect visível (via inspeção de código ou teste de animação básico).
  </acceptance_criteria>
  <done>Skeletons implementados.</done>
</task>

<task type="auto">
  <name>Task 2: Preparar index.tsx para Dados Dinâmicos</name>
  <read_first>AB0-1-mobile/src/app/index.tsx, AB0-1-mobile/src/lib/queries/home.ts</read_first>
  <files>AB0-1-mobile/src/app/index.tsx</files>
  <action>
    Integrar a nova query `GET_HOME_DATA` no `index.tsx`.
    - Substituir as múltiplas chamadas de `useQuery` por uma única chamada usando a query centralizada.
    - Mapear os resultados para os estados locais, mantendo compatibilidade com as props esperadas pelos componentes atuais.
    - Implementar lógica de renderização condicional: se `loading`, exibir `HomeSkeleton`.
    - Remover as guards de feature flag `EXPO_PUBLIC_GRAPHQL_HOME_ENABLED` (tornar o GraphQL o padrão).
  </action>
  <verify>
    <automated>grep "GET_HOME_DATA" AB0-1-mobile/src/app/index.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `index.tsx` utiliza `GET_HOME_DATA`.
    - `HomeSkeleton` é exibido durante o carregamento inicial.
    - O código compila sem erros de tipagem Apollo.
  </acceptance_criteria>
  <done>Main screen preparada para o fluxo dinâmico.</done>
</task>

</tasks>

<verification>
Verificar se os Skeletons aparecem corretamente simulando um delay na API.
</verification>

<success_criteria>
A Home exibe placeholders visuais durante o carregamento e as queries GQL estão prontas para alimentar os componentes reais nas próximas ondas.
</success_criteria>

<artifacts_produced>
- AB0-1-mobile/src/lib/queries/home.ts
- AB0-1-mobile/src/components/skeleton/HomeSkeleton.tsx
- AB0-1-mobile/src/components/skeleton/CompanyCardSkeleton.tsx
</artifacts_produced>

<output>
After completion, create `.planning/phases/01-funda-o-e-integra-o-da-home-p0/01-01-SUMMARY.md`
</output>
